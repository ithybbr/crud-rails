<%= form_with model: patientdisease,
  url: @patientdisease.persisted? ? patientdisease_path(email: @patientdisease.email) : patientdisease,
 method: @patientdisease.persisted? ? :patch : :post do |form| %>
  <% if patientdisease.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(patientdisease.errors.count, "error") %> prohibited this patientdisease from being saved:</h2>

      <ul>
        <% patientdisease.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :disease_code, style: "display: block" %>
    <%= form.text_field :disease_code %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
