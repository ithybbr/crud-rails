<%= form_with(model: publicservant) do |form| %>
  <% if publicservant.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(publicservant.errors.count, "error") %> prohibited this publicservant from being saved:</h2>

      <ul>
        <% publicservant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :department, style: "display: block" %>
    <%= form.text_field :department %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
